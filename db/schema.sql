-- Need to create 3 tables in a database

-- DROP DATABASE IF EXISTS
-- CREATE DATABASE
-- USE database_name

-- create DEPARTMENT TABLE
    -- id (int)
    -- name VARCHAR(30)

-- CREATE ROLE TABLE
    -- id (INT)
    -- title VARCHAR(30)
    -- salary (DECIMAL)
    -- department_id (int) -- THIS IS A FOREIGN KEY****

--  CREATE EMPLOYEE TABLE
    -- id (INT)
    -- first_name VARCHAR(30)
    -- last_name VARCHAR(30)
    -- role_id(INT) -- THIS IS A FOREIGN KEY ***
    --  manager_id -- THIS IS A FOREIGN KEY ****

